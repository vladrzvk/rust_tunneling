[package]
name = "tunneling"
version = "0.1.0"
edition = "2021"

[dependencies]
tokio = { version = "1", features = ["full"] }
log = "0.4"
env_logger = "0.9"
# Clap si vous voulez un petit CLI:
clap = { version = "4.3", features = ["derive"] }

# Optionnel si vous voulez compresser / manipuler des fichiers plus tard:
# flate2 = "1.0"